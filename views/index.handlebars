<h1>burgers!</h1>

<h2>burgers that are delicious!</h2>

<ul>
  {{#each burgers}}
    {{#unless hungry}}
    <li>
    not eaten:{{burger/burger}}
      {{!-- {{>burgers/burger=true}} --}}
      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2></h2>

<ul>
  {{#each burger}}
    {{this.burger_name}}

    {{#unless devoured}}
    <li>
    eaten: {{burger/burger}}
      {{!-- {{> burgers/burger=false}} --}}
      </li>
    {{/unless}}

    <button data-id="{{this.id}}" class="delquote">Delete</button>

    <a href="/{{this.id}}">Update this burger</a>
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgers">burgerName:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="hungry">Hungry?</label><br>
    <input type="radio" name="hungry" value="1" checked>Hungry!<br>
    <input type="radio" name="hungry" value="0"> Eat!
  </div>

  <button type="submit">Add Burger</button>
</form>
